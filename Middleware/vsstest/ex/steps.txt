sudo apt update
sudo apt install git cmake build-essential python3-pip libzmq3-dev
pip3 install pyyaml
git clone https://github.com/COVESA/vss-tools.git
cd vss-tools
pip3 install -r requirements.txt

Vehicle:
  Signal:
    Speed:
      type: sensor
      datatype: float
      unit: km/h
      description: "Vehicle speed in kilometers per hour."
    Battery:
      type: sensor
      datatype: float
      unit: percentage
      description: "Battery charge level as a percentage."

cd vss-tools
./vssgen --root ./vehicle.vspec --outdir ../vss_tree

find_package(ZeroMQ REQUIRED)
target_link_libraries(MyApp PRIVATE zmq)

